# ポケモンフレンダ ピックバトル アプリ 仕様書 v0.1

> ポケモンフレンダの「ピック（カード）」に書かれた **ポケモン名 / タイプ / エネルギー値** の3つだけを使って、
> 幼稚園児でも自分ひとりで遊べて、それでいて「ポケモンバトルらしさ」を味わえるデジタル・バトルアプリ。

---

## 1. コンセプト

| 項目 | 内容 |
| --- | --- |
| ターゲット | メイン: 4〜6歳（年中〜年長）／サブ: 一緒に遊ぶ保護者・兄姉 |
| プレイ形態 | 1台の端末を2人で交代して使う「ホットシート方式」（対面プレイ） |
| 1バトルの長さ | 2〜7分（3vs3でも約6分） |
| 読み書き前提 | **ひらがな・カタカナのみ**（漢字ゼロ）。読めなくても絵と音でわかる |
| 計算前提 | 足し算・引き算ができなくても遊べる（数値の増減はアプリが全部やる） |
| キモ | 「サイコロを振る」「だれをねらうか選ぶ」の2アクションだけ。それ以外は全部自動 |

### 設計の3原則

1. **えらぶのは2つだけ** — 「じぶんのポケモン」と「あいてのポケモン」。あとはサイコロを振るだけ。
2. **まちがえても損しない** — 選択は必ず取り消せる。理不尽な負け筋（0ダメージ・行動不能）は作らない。
3. **かならず盛り上がる山場がある** — ピンチのときにメガシンカで逆転できる。負けている側ほど強くなる設計。

---

## 2. 用語

| 用語（アプリ表記） | 内部名称 | 意味 |
| --- | --- | --- |
| ピック | `Pick` | ポケモンフレンダの実物カード。1枚 = 1ポケモン |
| ずかん | `PickBook` | 端末に登録したピックの一覧。ここから選んでチームを組む |
| たいりょく | `hp` | エネルギー値をそのままライフポイントとして使う（150〜350程度） |
| ばとるチーム | `Team` | 1〜3体のポケモン編成 |
| ひんし | `fainted` | たいりょくが0になった状態。以後、行動も攻撃対象にもならない |
| メガシンカ | `megaEvolved` | 条件を満たすと発動。サイコロが2個になる |

---

## 3. ゲームルール

### 3.1 バトル準備

1. **人数をえらぶ** — 1vs1 / 2vs2 / 3vs3 の3種類。
2. **チームをえらぶ** — プレイヤー1 → プレイヤー2 の順に、ずかんから人数分のポケモンを選ぶ。
   - 相手の選択画面に移る前に「はい、つぎのひとにわたしてね」という**受け渡し画面**を挟む（覗き見防止 & 交代の合図）。
3. **コイントス** — 画面のコインをタップすると回転アニメーション。表なら先攻、裏なら後攻。
   - 表 = モンスターボールの絵、裏 = ポケモンの足あと の絵。「どっちがでるかな？」と選ばせてから振る形式にして、当事者感を出す。

### 3.2 バトルフィールドの考え方（重要な設計判断）

**チーム全員が最初から場に出る「勢ぞろい方式」**を採用する。控え・交代の概念は持たない。

- 理由: 「攻撃する自分のポケモンと、攻撃対象の相手のポケモンを選ぶ」という要望を素直に実現できる。
- 理由: 「交代」は園児には損得の判断が難しく、ターンを消費する概念自体が理解しにくい。
- 理由: 3体が画面に並んでいる方が、自分の手持ちを見渡せて楽しい。
- 副次効果: 「弱った相手にトドメを刺す」「タイプ相性の良い組み合わせを探す」という、園児にも理解できる戦術が自然に生まれる。

```
   ┌─────────  あいて  ─────────┐
   │  [ピカチュウ]  [リザードン]  [カビゴン]  │   ← タップで攻撃対象を選ぶ
   ├──────────────────────────┤
   │           🎲 サイコロ           │
   ├──────────────────────────┤
   │  [フシギダネ]  [ゲンガー]   [ミミッキュ]  │   ← タップで攻撃するポケモンを選ぶ
   └─────────  じぶん  ─────────┘
```

### 3.3 1ターンの流れ

| # | ステップ | 画面の動き | 誰がやる |
| --- | --- | --- | --- |
| 1 | こうげきするポケモンをえらぶ | 自分の生存ポケモンが光る → タップで選択 | 手番プレイヤー |
| 2 | ねらうポケモンをえらぶ | 相手の生存ポケモンが光る → タップで選択。**「ばつぐん!」バッジが自動表示される** | 手番プレイヤー |
| 3 | サイコロをふる | サイコロを大きくタップ（または端末を振る）→ 回転アニメ | 手番プレイヤー |
| 4 | ダメージけいさん | 出目 → 倍率 → ダメージ数値が飛んでいく演出 | 自動 |
| 5 | たいりょくバーがへる | バーが減るアニメ + 効果音 | 自動 |
| 6 | ひんしはんてい | 0になったら「たおれた！」演出 | 自動 |
| 7 | メガシンカはんてい | 条件を満たしたポケモンがいれば発動演出 | 自動 |
| 8 | こうたい | 「つぎは 〇〇のばん！」→ 端末を渡す | 自動 |

**ステップ1・2はいつでもやり直せる**（サイコロを振るまでは選択解除可能）。

### 3.4 ダメージ計算

```
ダメージ = サイコロの出目の合計 × ダメージばいりつ × タイプ補正
```

| 要素 | 値 |
| --- | --- |
| サイコロの出目 | 通常 1個（1〜6）／メガシンカ中 2個（2〜12） |
| ダメージばいりつ | **×20（デフォルト「ふつう」）** — 設定で ×10「じっくり」／×30「さくさく」に変更可 |
| タイプ補正 | こうかばつぐん = **2ばい**／それ以外 = 1ばい |

- 表示は必ず**整数**（10の倍数）になる。小数・端数は絶対に出さない。
- **「いまひとつ」「こうかがない」は実装しない。** 半分にする計算は園児に理解しづらく、0ダメージは「何もできなかった」体験になって楽しくないため。**ボーナスだけがある**設計にする。

#### なぜ ×20 なのか（バランス検証済み）

要望の「出目×10」で 20,000 回シミュレーションした結果、3vs3 が **平均31.9ターン（最大58ターン、約10〜20分）** となり、園児の集中力が持たない長さになる。倍率を上げて再検証した結果が下表。

| ばいりつ | 1vs1 | 2vs2 | 3vs3 | 3vs3の想定プレイ時間 |
| --- | --- | --- | --- | --- |
| ×10「じっくり」 | 平均9.9ターン | 平均20.7ターン | 平均31.9ターン（最大58） | 約10〜20分 |
| **×20「ふつう」（推奨）** | **平均5.1ターン** | **平均11.2ターン** | **平均17.4ターン（最大33）** | **約6分** |
| ×30「さくさく」 | 平均3.5ターン | 平均7.9ターン | 平均12.5ターン（最大24） | 約4分 |

> 条件: たいりょく150〜350、こうかばつぐん15.7%、メガシンカ保有率40%、1ターン20秒換算、各20,000試行。

×20 なら 1vs1 が約1.7分で「もう1回やろう！」を誘発し、3vs3 でも約6分と園児の集中が切れる前に決着する。**倍率は設定画面で変更できる**ようにし、慣れてきたら ×10 でじっくり遊べる余地も残す。

### 3.5 タイプ相性

**子どもは相性表を覚えなくてよい。** 攻撃対象を選んだ瞬間に、アプリが「⚡ ばつぐん!」バッジと効果音で教えてくれる。遊んでいるうちに自然に覚える、という体験を狙う。

判定は本家準拠の「こうかばつぐん」のみ（1体1タイプとして扱い、ピックに複数タイプが書かれている場合は登録時に第1タイプを主タイプとする）。

| こうげきタイプ | ばつぐんになる相手 |
| --- | --- |
| ノーマル | （なし） |
| ほのお | くさ・こおり・むし・はがね |
| みず | ほのお・じめん・いわ |
| でんき | みず・ひこう |
| くさ | みず・じめん・いわ |
| こおり | くさ・じめん・ひこう・ドラゴン |
| かくとう | ノーマル・こおり・いわ・あく・はがね |
| どく | くさ・フェアリー |
| じめん | ほのお・でんき・どく・いわ・はがね |
| ひこう | くさ・かくとう・むし |
| エスパー | かくとう・どく |
| むし | くさ・エスパー・あく |
| いわ | ほのお・こおり・ひこう・むし |
| ゴースト | エスパー・ゴースト |
| ドラゴン | ドラゴン |
| あく | エスパー・ゴースト |
| はがね | こおり・いわ・フェアリー |
| フェアリー | かくとう・ドラゴン・あく |

### 3.6 メガシンカ

バトル中の山場を作る、逆転のための仕組み。

| 項目 | 内容 |
| --- | --- |
| 発動条件 | **自分のポケモンのたいりょくが半分以下になったとき**、自動で発動 |
| 対象 | ずかん登録時に「メガシンカできる」にチェックを入れたポケモンのみ |
| 回数 | 1バトルにつき、そのポケモン1回だけ（元に戻らない） |
| 効果 | **サイコロが2個になる**（ダメージ期待値が2倍） |
| 演出 | 画面が光る → 専用BGM/効果音 → ポケモンの枠が虹色に変化 → 「メガシンカ！」の大きな文字 |

- 「ピンチになったら強くなる」＝負けている側が強くなるため、**試合が最後まで盛り上がる**（ラバーバンド効果）。
- 条件が「半分以下」だけなので、園児が能動的に判断・操作する必要がない。

#### 将来の拡張余地（v0.1 では実装しない）
- ゾロ目で「きゅうしょにあたった！」→ もう1回振れる
- 出目6で +20 ボーナス
- ひんしになった味方の「おうえん」で +10

### 3.7 勝敗

- 相手チームの**全ポケモンがひんし**になったら勝ち。
- 結果画面では「かった！」を大きく祝う一方で、**負けた側にも必ず「〇〇は さいごまで がんばった！」と MVP ポケモンを表示**する（最も多くダメージを与えたポケモン）。園児が泣かないための配慮。
- 「もういっかい」ボタンで同じチーム編成のまま即リマッチできる。

---

## 4. 幼稚園児向け UX 要件

これは「あったら良い」ではなく**必須要件**として扱う。

| # | 要件 | 詳細 |
| --- | --- | --- |
| U-1 | 文字はすべて ひらがな・カタカナ | 漢字を使わない。フォントは丸ゴシック系、最小 20px 相当 |
| U-2 | タップ領域は最低 64×64 px | 指が太くても押し間違えない |
| U-3 | 読み上げ | 主要メッセージは音声読み上げ（Web Speech API）。文字が読めなくても進行できる |
| U-4 | 常に「つぎ、なにをするか」がわかる | 操作すべき対象は必ず光る・跳ねるアニメーションをつける。それ以外は暗くする |
| U-5 | 待ち時間ゼロの手ざわり | すべてのタップに即座に音とアニメで反応する |
| U-6 | 取り消しできる | サイコロを振るまで選択はやり直せる。「もどる」ボタンを常設 |
| U-7 | 数字は見た目でも伝える | たいりょくは数値 + バー + 色（緑→黄→赤）の3重表現 |
| U-8 | 敗北をやわらげる | 「まけ」より「よくがんばったで賞」を強調。ひんし演出は暗くしすぎない |
| U-9 | 保護者用の入口を分ける | ずかん登録・設定は長押し3秒で入る（園児が誤って設定を壊さない） |
| U-10 | 音のオンオフ | 電車内など無音で遊べるよう、常時アクセスできるミュートボタン |

---

## 5. 画面仕様

### 5.1 画面一覧

| ID | 画面名（表示名） | 役割 |
| --- | --- | --- |
| S-1 | タイトル | 「ばとるを はじめる」「ずかん」 |
| S-2 | ずかん | 登録済みピック一覧・追加・編集・削除（保護者向け） |
| S-3 | ピックとうろく | ポケモン名 / タイプ / エネルギー値 / メガシンカ可否 を入力 |
| S-4 | にんずうえらび | 1vs1 / 2vs2 / 3vs3 |
| S-5 | チームえらび | ずかんから人数分を選択（プレイヤーごとに2回） |
| S-6 | こうたい | 「つぎの ひとに わたしてね」 |
| S-7 | コイントス | 先攻決定 |
| S-8 | バトル | メイン画面 |
| S-9 | けっか | 勝敗表示・MVP・もういっかい |
| S-10 | せってい | ダメージばいりつ・音量・読み上げ ON/OFF |

### 5.2 画面遷移

```
S-1 タイトル
 ├─ ずかん ──→ S-2 ずかん ⇄ S-3 ピックとうろく
 ├─ せってい ─→ S-10 せってい          （長押し3秒で入る）
 └─ はじめる ─→ S-4 にんずうえらび
                  ↓
                S-5 チームえらび（P1） → S-6 こうたい → S-5 チームえらび（P2）
                  ↓
                S-7 コイントス
                  ↓
                S-8 バトル ⇄ S-6 こうたい（ターンごと）
                  ↓
                S-9 けっか ──→「もういっかい」で S-7 へ／「さいしょから」で S-1 へ
```

### 5.3 バトル画面（S-8）の構成要素

| 領域 | 内容 |
| --- | --- |
| 上部 | 相手チーム（1〜3体のカード）。カード内: ポケモン名 / タイプアイコン / たいりょくバー / 数値 |
| 中央 | サイコロ（大）／メッセージ帯（「だれで こうげきする？」等）／ばつぐんバッジ |
| 下部 | 自分チーム（1〜3体のカード） |
| 常設 | もどるボタン、ミュートボタン、いまのターン表示 |

---

## 6. データモデル（TypeScript）

```ts
/** ポケモンのタイプ（本家18タイプ） */
type PokemonType =
  | 'ノーマル' | 'ほのお' | 'みず' | 'でんき' | 'くさ' | 'こおり'
  | 'かくとう' | 'どく' | 'じめん' | 'ひこう' | 'エスパー' | 'むし'
  | 'いわ' | 'ゴースト' | 'ドラゴン' | 'あく' | 'はがね' | 'フェアリー';

/** ずかんに登録された1枚のピック */
interface Pick {
  id: string;
  name: string;            // ポケモン名（カタカナ）
  type: PokemonType;       // 主タイプ
  energy: number;          // エネルギー値 = 初期たいりょく（150〜350程度）
  canMegaEvolve: boolean;  // メガシンカできるか
  photoDataUrl?: string;   // 実物ピックの写真（任意・localStorage に保存）
}

/** バトル中のポケモンの状態 */
interface BattlePokemon {
  pickId: string;
  name: string;
  type: PokemonType;
  maxHp: number;           // = Pick.energy
  hp: number;              // 0 になったら ひんし
  canMegaEvolve: boolean;
  megaEvolved: boolean;    // メガシンカ済みか
  damageDealt: number;     // MVP 判定用の累計与ダメージ
}

type PlayerId = 'p1' | 'p2';

interface BattleState {
  teams: Record<PlayerId, BattlePokemon[]>;
  turnPlayer: PlayerId;
  turnCount: number;
  phase: 'selectAttacker' | 'selectTarget' | 'rollDice' | 'resolve' | 'handOff' | 'finished';
  selectedAttackerIndex: number | null;
  selectedTargetIndex: number | null;
  lastRoll: number[];      // 直近の出目（メガシンカ中は2要素）
  winner: PlayerId | null;
}

interface Settings {
  damageMultiplier: 10 | 20 | 30;  // デフォルト 20
  soundEnabled: boolean;
  speechEnabled: boolean;
}
```

### 6.1 ダメージ計算関数（仕様の中核）

```ts
function calcDamage(
  attacker: BattlePokemon,
  target: BattlePokemon,
  rolls: number[],
  multiplier: number,
): { damage: number; isSuperEffective: boolean } {
  const diceSum = rolls.reduce((a, b) => a + b, 0);
  const isSuperEffective = TYPE_CHART[attacker.type].includes(target.type);
  const damage = diceSum * multiplier * (isSuperEffective ? 2 : 1);
  return { damage, isSuperEffective };
}
```

- 振るサイコロの数 = `attacker.megaEvolved ? 2 : 1`
- 適用後 `target.hp = Math.max(0, target.hp - damage)`
- `target.hp === 0` → ひんし
- 攻撃後、**自分側の全ポケモン**について `canMegaEvolve && !megaEvolved && hp <= maxHp / 2` を判定して自動メガシンカ

---

## 7. 技術構成

| 項目 | 選定 | 理由 |
| --- | --- | --- |
| 形態 | Web アプリ（PWA） | インストール不要、iPad/スマホ/PC どれでも動く。ホーム画面に追加すればアプリらしく使える |
| フレームワーク | React + TypeScript | 状態管理が素直。型でルールのミスを防ぐ |
| ビルド | Vite | 起動が速く設定が少ない |
| 状態管理 | `useReducer` + Context | バトル進行は状態機械なので reducer と相性が良い |
| 永続化 | localStorage | ずかん・設定を保存。サーバー不要 |
| 通信 | なし（完全オフライン） | 子ども向けアプリとして、個人情報も通信も持たない設計にする |
| 音声 | Web Audio API + Web Speech API | 効果音と読み上げ |
| テスト | Vitest | ダメージ計算・勝敗判定・メガシンカ条件はユニットテスト必須 |

### 7.1 想定ディレクトリ構成

```
src/
├─ domain/          # ルールの実装（UIから独立、テスト対象）
│  ├─ typeChart.ts
│  ├─ damage.ts
│  ├─ battleReducer.ts
│  └─ __tests__/
├─ screens/         # 画面コンポーネント（S-1〜S-10）
├─ components/      # カード・サイコロ・たいりょくバー等
├─ storage/         # localStorage 読み書き
└─ audio/           # 効果音・読み上げ
```

**ルール（domain）は UI から完全に分離する。** バランス調整のたびに UI を触らずに済み、シミュレーションでの再検証も容易になる。

---

## 8. 開発フェーズ

| フェーズ | 内容 | 完了条件 |
| --- | --- | --- |
| **P0: ルールの土台** | 型定義・タイプ相性表・ダメージ計算・バトル reducer + ユニットテスト | テストが全部通る。UI なしで1バトル完走できる |
| **P1: 遊べる最小版（MVP）** | S-1/S-4/S-5/S-7/S-8/S-9、ずかんは仮データ、1vs1 のみ | 大人が1vs1を最後まで遊べる |
| **P2: ずかん** | S-2/S-3、localStorage 保存、2vs2/3vs3 対応 | 実物のピックを登録して3vs3ができる |
| **P3: 園児向け仕上げ** | 音・読み上げ・アニメーション・ひらがな徹底・タップ領域・受け渡し画面 | **園児に実際に遊んでもらい、大人の助けなしで1バトル完走できる** |
| **P4: 調整** | 設定画面、バランス倍率、MVP表示、PWA 化 | ホーム画面から起動できる |

P3 の完了条件は必ず**実際の子どもによるプレイテスト**で判定する。ここが本アプリの成否を分ける。

---

## 9. 将来の拡張候補

- **ピックのカメラ読み取り** — 実物カードを撮影して名前・タイプ・エネルギー値を OCR で自動入力（登録の手間がゼロになる）
- **とくせい / わざ** — ピックに他の情報が書かれていれば、それを使った追加効果
- **オンライン対戦** — 離れた友達と遊ぶ
- **バトルきろく** — 勝敗履歴、よく使うポケモンのランキング
- **きょうりょくバトル** — 大人と子どもがチームを組んで CPU と戦う
- **CPU 対戦** — ひとりでも遊べるモード（難易度: よわい / ふつう）

---

## 10. 未決事項（要確認）

| # | 論点 | 現時点の案 |
| --- | --- | --- |
| Q-1 | ピックに複数タイプが書かれている場合の扱い | 登録時に第1タイプを主タイプとして選ぶ（v0.1）。将来は2タイプ判定に拡張 |
| Q-2 | ダメージばいりつのデフォルト | ×20。実際に遊んでみて長ければ ×30 に変更 |
| Q-3 | メガシンカ発動条件 | 「たいりょく半分以下」。「ゾロ目が出たら」等の案もあるが、園児の操作不要な自動発動を優先 |
| Q-4 | ずかんの登録件数上限 | localStorage 容量の都合上、写真つきなら 50 件程度を想定 |
| Q-5 | 同じピックを両プレイヤーが使えるか | 使える（ミラーマッチ可）。ピックが少ない家庭でも遊べるようにするため |

---

*本仕様は v0.1（たたき台）です。P3 のプレイテスト結果を受けて、ダメージ倍率・メガシンカ条件・演出の長さを見直す前提で書かれています。*
